<Window x:Class="TaskTool.Views.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task Erinnerung" Height="160" Width="320" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Topmost="True">
    <StackPanel Margin="12">
        <TextBlock Text="Task startet bald:" FontWeight="Bold" />
        <TextBlock x:Name="TaskLabel" Margin="0,8,0,0" TextWrapping="Wrap" />
        <TextBlock x:Name="StartLabel" Margin="0,4,0,12" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Snooze 5 min" Width="100" Margin="0,0,8,0" Click="Snooze_Click" />
            <Button Content="Schließen" Width="80" Click="Close_Click" />
        </StackPanel>
    </StackPanel>
</Window>
