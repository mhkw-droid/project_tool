<UserControl x:Class="TaskTool.Views.WeekView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid Background="{StaticResource BgBrush}">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <DockPanel LastChildFill="False">
                <Button Content="← Vorherige Woche" Width="150" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding PreviousWeekCommand}"/>
                <Button Content="Diese Woche" Width="120" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CurrentWeekCommand}"/>
                <Button Content="Nächste Woche →" Width="150" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding NextWeekCommand}"/>
                <TextBlock Text="{Binding WeekRangeLabel}" FontSize="22" FontWeight="SemiBold" Margin="14,4,0,0"/>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="Transparent">
            <ItemsControl ItemsSource="{Binding Days}">
                <ItemsControl.ItemsPanel><ItemsPanelTemplate><UniformGrid Rows="1" Columns="7"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,10,12" MinWidth="230">
                            <StackPanel>
                                <TextBlock Text="{Binding DayLabel}" FontSize="17" FontWeight="Bold"/>
                                <WrapPanel Margin="0,4,0,4">
                                    <Border Background="#374151" CornerRadius="6" Padding="6,2" Margin="0,0,4,0"><TextBlock Text="{Binding DayType}" FontSize="11"/></Border>
                                    <Border Background="#1E3A8A" CornerRadius="6" Padding="6,2" Margin="0,0,4,0" Visibility="{Binding IsHo, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Text="HO" FontSize="11"/></Border>
                                    <Border Background="#7C2D12" CornerRadius="6" Padding="6,2" Margin="0,0,4,0" Visibility="{Binding IsBr, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Text="BR" FontSize="11"/></Border>
                                </WrapPanel>
                                <TextBlock Text="{Binding Summary}" Foreground="{StaticResource MutedTextBrush}" FontSize="12" Margin="0,0,0,6"/>

                                <ItemsControl ItemsSource="{Binding Tasks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#0B1220" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1" CornerRadius="8" Padding="8" Margin="0,0,0,8">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding StartLocal, StringFormat={}{0:HH:mm}}" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Text="Keine Tasks" Foreground="{StaticResource MutedTextBrush}" Visibility="Collapsed">
                                    <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Tasks.Count}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
