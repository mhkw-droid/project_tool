<Window x:Class="TaskTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TaskTool" Height="760" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="#1F2937" Grid.Column="0">
            <StackPanel Margin="8">
                <TextBlock Text="TaskTool" Foreground="White" FontSize="22" FontWeight="Bold" Margin="8" />
                <ListBox ItemsSource="{Binding NavigationItems}" SelectedItem="{Binding SelectedView}" Margin="8">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" Foreground="White" Margin="8" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding SelectedView}" />
    </Grid>
</Window>
